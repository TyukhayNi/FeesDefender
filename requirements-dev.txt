# Dependencias de desarrollo (no de runtime). Instalar: pip install -r requirements-dev.txt
# Barrera local anti-fugas — ver docs/SEGURIDAD_DATOS.md
pre-commit>=4.6
# Requerido por los tests de los servers MCP (plugins/*_mcp, plugins/expedientes_xl)
mcp>=1.28,<2   # 1.x: los cuatro servers MCP usan mcp.server.fastmcp, retirado en 2.0.
               # Sin techo, este mismo fichero reinstala la averia del 2026-08-23 en
               # el venv que los wrappers PREFIEREN. Ver docs/DEAD_ENDS.md.
# Requerido para fabricar PDFs con capa de texto en 4 ficheros de test: test_ocr_escalera,
# test_pdf_paginas, test_sala_maquina_ejecutar y test_sala_maquina_escalera. Sin el paquete caen
# 37 tests (medido 2026-08-25); un venv montado solo con requirements.txt los rompe todos.
reportlab>=4.0
