{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Configuración local de Nikolai. NO se versiona (en .gitignore). Sobreescribe settings.json.",
  "env": {
    "_comment_env": "Aquí puedes meter overrides puntuales. Las credenciales reales viven en .env, no aquí."
  },
  "permissions": {
    "_comment_permissions": "Si en algún momento necesitas permitir algo más restrictivo solo para tu máquina, añádelo aquí. Ejemplo: Bash(npm install -g *) si vas a instalar tooling global."
  }
}
